int main(){
    /*
        after we give an identifier to an object(variable) we needa also tell the program how to interpret the value store in the variable at runtime
        in simple words we gonna tell the compiler the type of data that we are going to store in the variable so that the program can interpret the data accordingly
    */

   // all valid definitions
   int x; int y; int z;  // tells the compiler that we are going to store integer value in the variables (x,y,z)
   int a;  // same int
   int b; double c;  // tells the compiler that b is of int type and double is of double type(integer containing fractional part)
   double db, dbb;  // tels that db, dbb both are of double type
}