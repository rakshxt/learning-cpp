#include <iostream>
#include <cstdint>
#include <cstdlib>

/*
    a constexpr function is a function whose return value is known at compile-time and hence, all the future observance of that function would be replaced by that value by the compiler
    this way, we can somehow, improve the performance of the program.
*/


int main(int argc, char const *argv[]){

    return EXIT_SUCCESS;
}
